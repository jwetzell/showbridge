---
kind: pipeline
name: generate-schema-docs

steps:
  - name: generate schema docs
    image: python:3
    commands:
      - pip install json-schema-for-humans
      - mkdir -p html/docs/schema
      - generate-schema-doc --config link_to_reused_ref=false src/schema/config.schema.json html/docs/schema/index.html
  - name: deploy schema docs
    image: cschlosser/drone-ftps
    environment:
      FTP_USERNAME:
        from_secret: ftp_username
      FTP_PASSWORD:
        from_secret: ftp_password
      PLUGIN_HOSTNAME: ftp.jwetzell.com:21
      PLUGIN_SRC_DIR: /html/docs/schema
      PLUGIN_DEST_DIR: /docs/schema
      PLUGIN_SECURE: true
      PLUGIN_VERIFY: false
      PLUGIN_CLEAN_DIR: true
